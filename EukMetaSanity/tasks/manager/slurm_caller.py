import os
import logging
from plumbum import local, BG
from typing import Dict, List, Tuple, Callable
from plumbum.machines.local import LocalCommand, LocalMachine


class SLURMCaller:
    def __init__(self, cmd: LocalCommand):
        pass

    def __str__(self):
        pass

    def __call__(self, *args, **kwargs):
        pass
